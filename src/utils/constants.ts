// App constants
export const APP_NAME = 'VibeSnap';
export const POSTS_PER_PAGE = 20;

// Firebase collection names
export const COLLECTIONS = {
  POSTS: 'posts',
  USERS: 'users',
} as const;

// Storage paths
export const STORAGE_PATHS = {
  POSTS: 'posts',
  AVATARS: 'avatars',
} as const;